load("@bazel_gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/cncf/xds
# gazelle:proto disable_global
gazelle(name = "gazelle")

# Create a target for coordinating buf generation and Gazelle updates
sh_binary(
    name = "update_buf_and_gazelle", 
    srcs = ["tools/update_buf_and_gazelle.sh"],
    visibility = ["//visibility:public"],
)