name: Python Package Release

on:
  push:
    tags:
      - 'python/v*'

jobs:
  release:
    name: Build and Publish Python Package
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Extract version from tag
        id: get_version
        run: |
          VERSION=${GITHUB_REF#refs/tags/python/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Update version in pyproject.toml
        run: |
          cd python
          sed -i "s/version = \".*\"/version = \"${{ steps.get_version.outputs.version }}\"/" pyproject.toml
          cat pyproject.toml

      - name: Build package
        run: |
          cd python
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: python/dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Python Release ${{ steps.get_version.outputs.version }}
          body: |
            Python package release version ${{ steps.get_version.outputs.version }}
            
            ## Installation
            ```bash
            pip install xds==${{ steps.get_version.outputs.version }}
            ```
          files: python/dist/*
